const errors = {
  emailError: {
    error: "Email is invalid",
    invalid: false,
  },
  passwordError: {
    error: "Password should be at least 8 characters long",
    invalid: false,
  },
};

export default errors;
