// TODO: Update this

/**
 * @arugment {String} text
 * @arugment {String} id
 * @param {{ type: "media" | "text" }}
 */

const Post = ({ text, id, type }) => {
  return <div></div>;
};

export default Post;
